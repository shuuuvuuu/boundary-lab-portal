RETICULUM_WS_URL=ws://reticulum.hcce.svc.cluster.local:4000/socket
RETICULUM_DB_URL=postgres://ro_user:password@reticulum-postgres:5432/ret_production
RETICULUM_BOT_ACCESS_KEY=replace-me

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=replace-me

DISCORD_ALERT_WEBHOOK_URL=
SIDECAR_LOG_LEVEL=info
SIDECAR_RECONNECT_MAX_MS=60000
SIDECAR_HEALTH_PORT=8080

# Optional: override if the Reticulum schema differs from the default hubs query.
# RETICULUM_HUBS_QUERY=select hub_sid from hubs where entry_mode not in ('deny','allow') and coalesce(soft_deleted,false) = false
