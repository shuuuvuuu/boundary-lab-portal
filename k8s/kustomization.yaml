apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: hcce
resources:
  - deployment.yaml
  - ingress.yaml
  - hubs-entry-sidecar.yaml
# secret.example.yaml は参照用。実値は kubectl create secret で hcce に直接投入する。
