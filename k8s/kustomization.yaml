apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: portal
resources:
  - namespace.yaml
  - deployment.yaml
  - ingress.yaml
# secret.example.yaml は参照用。実値は kubectl create secret で別途投入する。
